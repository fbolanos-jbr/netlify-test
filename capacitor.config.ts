import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'somos-antigua',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
